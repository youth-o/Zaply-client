import ContentToggle from "./ContentToggle";

const MainContent = () => {
  return (
    <main className="flex flex-col gap-4">
      <ContentToggle />
    </main>
  );
};

export default MainContent;
